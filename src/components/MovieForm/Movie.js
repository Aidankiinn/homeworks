import React, {Component} from 'react';
import Form from "../Form/Form";

class Movie extends Component {
    render() {
        return <Form title="Delete" type="button"/>;
    }
}

export default Movie;